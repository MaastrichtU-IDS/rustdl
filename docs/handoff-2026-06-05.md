# Handoff — 2026-06-05

Engine + infra session. All work landed on `main` via PRs #2–#11 (squash),
each green on CI, FP=0 preserved throughout (closure-diff gated).

## Shipped

| PR | Area | Summary |
|----|------|---------|
| #2 | python | Offline gzip-bundled example ontologies (pizza/SULO/SIO); `rustdl.examples.*` decompress-to-cache. |
| #3 | tableau (wedge) | **Increment 1** — algebraic cardinality *clash* pre-check: `≤n` with > n pairwise must-distinct successors ⇒ `Unsat` with no merge branching. PolloAdAstra⊑InterestingPizza 20 973→1 281 branches; `match_attempts` −32%. 11/11 closures byte-identical. |
| #4 | tableau (wedge) | **Increment 2** — canonical partition enumeration for the `≤n` merge rule (dedup the merge-order redundancy). SpicyPizza⊑InterestingPizza Stalled→Sat (merge branches 70 377→1); probe stalls 1→0; wall 13.6 s→7.7 s. 10/10 closures identical. |
| #5 | saturation | Detect unsat from `SubClassOf(X, …¬Atomic(Y)…)` complement conjuncts (register `disjoint(X,Y)`). Closes the Horn-fast-path unsat gap (`A⊑B⊓¬B`). 8/8 closures identical. |
| #6 | reasoner (tests) | Pin `RUSTDL_HORN_SHORTCIRCUIT`/`RUSTDL_SNAPSHOT_CAPTURE` in 6 stale classify tests (broken by default-flag flips, not bugs); shared `test_env_lock` for parallel-safety. |
| #10 | reasoner (abox) | **P8 functional-collapse** + inverse-derived domain/range in the A1 pre-check. Sound capability gain; **does NOT close family** (deep multi-step graph entailment = tableau-scale). |
| #11 | tableau | **Fix `parent-pointer cycle`** (TODO(blocking-cycle)): `merge_into` re-parenting could point an ancestor at its descendant. Break the back-edge (child→root) when re-parent would cycle. Fixed the SIO closure-diff panic; full SROIQ parity preserved. |
| #7,#8,#9 | ci | rustfmt-1.9.0 baseline; green clippy/build&test/cargo-deny; **pin toolchain to 1.95.0** (`rust-toolchain.toml` + action) so drift can't recur. |

## Toolchain (important)

CI and local are now pinned to **Rust 1.95.0** (`rust-toolchain.toml` +
`dtolnay/rust-toolchain@<sha>` with explicit `toolchain:`). Bump deliberately
in a dedicated PR — keep `rust-toolchain.toml` and the four `ci.yml`
`toolchain:` inputs in sync. CI is fully green on this pin.

## MISS probe — remaining completeness gaps are research-grade

Probed whether any remaining MISS is a bounded fix. Conclusion: **no.**

- **notgalen MISSED = 18** — one structural cluster: 9 cardiac-condition
  classes each missing the same 2 supers (`Anonymous-324`,
  `IntrinsicallyPathologicalBodyProcess` = the "IPBP cluster"). notgalen is
  Horn ⇒ classify trusts the saturation closure, which misses these; the full
  wedge/tableau path recovers them but **times out >30 s per pair**. Already
  attacked: Phase 2c sub-role witness propagation **reverted at 0/44**; Phase
  2d recovered 9 (27→18). The remaining 18 need the saturation-completeness +
  tableau-scale work. **Research-grade.**
- **SIO MISSED = 2** — `SIO_010092 ⊑ SIO_001353`, `⊑ SIO_010410` (one class,
  two supers; out-of-EL ⇒ wedge/`trust_sat` misses). Only became *measurable*
  after the #11 parent-pointer-cycle fix (the closure-diff used to panic).
  FP=0.
- **pizza / Konclude ≤5× on SROIQ** — the blocking lever: subset/pair-blocking
  barely fires on these model shapes, so completions grow. The
  `architecture-roadmap.md` headline lever; multi-week.

## Deferred levers (scoping-first, soundness-sensitive)

- **Stronger blocking** that fires on pizza/SIO-shaped models — the core lever
  behind the `trust_sat` MISSES and the Konclude perf gap. Multi-week.
- **Konclude-style sub-tableau caching** — structurally-different lever;
  uncertain-benefit, measure first.
- **Disjunction backjumping through merges** — the `≤n` merge path reports
  `DepSet::ALL`, defeating disjunction backjumping (SpicyPizza's residual
  ~50k disj branches). Perf-only; **soundness-risky** (under-reporting deps →
  false subsumption). See `memory` note.
- **family / family-stripped inconsistency** — tableau-scale (deep graph
  entailment), not a pre-check pattern; P8 catches the shallow variant only.

## Cheap carry-overs

- Rename `classify_pure_el` → `classify_via_saturation_closure` (+ the
  `pure_el_mode` flag) — misleading since Phase 2b widened it to Horn.
- The two untracked files (`docs/flamegraphs/go-classify-2026-05-25.svg`,
  `scripts/phase2b-build-fixtures.sh`) are still untracked — commit or
  `.gitignore` deliberately.
