//! Datatype reasoners.
//!
//! Phase 3 lands a minimal slice (`xsd:boolean`, `xsd:integer` with min/max
//! facets, `xsd:string` equality). Phase 7 expands to full concrete domains
//! (`xsd:decimal`, `xsd:double`, `xsd:dateTime`, pattern facets, ...).
//!
//! Currently empty.
