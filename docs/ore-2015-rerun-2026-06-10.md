# ORE 2015 re-measure — post D6–D11 (rustdl vs Konclude∩HermiT oracle)

Run date: 2026-06-10. Re-measure of the 2026-06-08 ORE run
(`docs/ore-2015-results-2026-06-08.md`) at repo HEAD `982b622`, after the
soundness/completeness work of this session:

- **snapshot-cache soundness fix** (`RUSTDL_SNAPSHOT_CAPTURE` default → OFF)
- **datatype value-membership** D6–D9 (integer/float/decimal/date/dateTime/string)
- **D10** Horn-shortcircuit gated on `saturator_complete_fragment` (∀/disjoint
  Horn ontologies route to the complete hybrid path)
- **D11** `DataAllValuesFrom` (∀p.DKey + global `DisjointClasses(DKey,DKey)`)

**Methodology.** The oracle (Konclude ∩ HermiT, per-ontology) is FIXED, so only
rustdl was re-run (current default config: snapshot OFF, trust_sat ON, label
heuristic ON; `--pair-timeout-ms 1000`, `timeout 300`) on each cached
`canon.owx`, then re-diffed with the unchanged `work/diff.py`. Prior `diff.json`
snapshotted to `diff.prev.json` for a per-ontology delta. Harness:
`work/rerun_rustdl.sh` + `work/rerun_aggregate.py` (in `/data/dumontier/ore-run`,
not committed).

## Coverage
233 pilot dirs: **186 diffed** (full oracle), 27 rustdl DNF (>300 s, heavy
tail), 20 skip (no cached oracle), 0 rustdl error.

## C1 — SOUNDNESS (the headline)
- **`FP_strict = 0` across all 186 diffed ontologies** (rustdl asserts nothing
  that NEITHER complete reasoner does). `FP` vs the oracle intersection also 0.
- The **6 ontologies that had output-level FP under the old snapshot-ON
  default are all cleaned up** (delta `prev>0 → now 0`): `ore_ont_9941` (62),
  `ore_ont_4897` (35), `ore_ont_13723` (30), `ore_ont_2232` (30),
  `ore_ont_5940` (4), `ore_ont_6423` (3). → the 2026-06-08 snapshot-cache
  unsoundness is **fixed at scale**.
- **Zero FP regressions.** This is the first at-scale exercise of D11's global
  `DisjointClasses(DKey,DKey)` seeding (every ORE ontology with multiple data
  values now runs `definitely_disjoint`) — it introduced no spurious ⊥. D8/D9
  likewise clean.

## C2 — completeness / silent misses
- **`ore_ont_9054` MISSED 79 → 0** (datatype value-membership fix, validated at
  scale). 7 ontologies MISSED-improved, **0 regressed** (no completeness loss
  from D10's saturation→hybrid reroute or D11's added axioms).
- **Silent-miss (MISSED>0 ∧ timed_out=0) — UPPER BOUND: 13 onts / 1514 pairs.**
  Output-level; includes trust_sat/label-masked misses, NOT only genuine
  calculus incompleteness (the prior "72 calculus_fn" was the mechanism-split
  number). Concentrated in 3 large ontologies:
  - `ore_ont_4827` (olia, out-of-EL): 781 — improved? (new vs prior)
  - `ore_ont_14450` (SWEET, Horn): 329 (was 586)
  - `ore_ont_13621` (SWEET, Horn): 323 (was 580)
  the remaining 10 onts total ~81 pairs, incl. the genuine cross-ontology BFO
  cluster `ore_ont_15273` / `ore_ont_6437` (SAO neuroscience, 5 each — the
  characterized tableau-calculus miss, unchanged) and a long tail of 1–13.
- **Diagnostic (trust_sat=0 on the 3 dominant onts + SAO 15273):** all four
  **DNF at 300 s** with the wedge accelerator off. Informative: under the
  default (trust_sat ON) these complete fast but SILENTLY miss (timed_out=0);
  with trust_sat OFF the full per-pair tableau exceeds budget → they would be
  **flagged-incomplete (timeout), not silent**. So the bulk of the 1514 upper
  bound is the **trust_sat-masking channel** (the wedge's `Sat` verdict
  standing in for an unfinished tableau under budget), NOT genuine calculus
  incompleteness the engine fundamentally cannot find. This is the C2
  architectural hole the 2026-06-08 matrix predicted ("12253's silent miss
  became a flagged timeout under TRUST_SAT=0").
## Stage-2 — genuine-calculus split (per-pair, accelerators OFF)

Each silently-missed pair re-checked with `rustdl subclass A B` under
`RUSTDL_HYPERTABLEAU_TRUST_SAT=0 RUSTDL_LABEL_HEURISTIC=0
RUSTDL_HORN_SHORTCIRCUIT=0` (snapshot already off), `timeout 30`/pair — so a
`no` reflects the full tableau, not a trust_sat shortcut. Big onts sampled at
CAP=100 pairs; the ≤34-pair onts adjudicated exhaustively. Harness:
`work/stage2_mech.sh`. Verdicts: **genuine** (`no` — oracle=yes, full
tableau=no = real calculus gap), **masked** (`yes` — full tableau derives it;
default classify missed it via trust_sat/orchestrator = recoverable),
**dnf** (>30 s = budget-unadjudicable).

**GENUINE calculus FN = 10 pairs / 2 ontologies** — `ore_ont_15273` (5) +
`ore_ont_6437` (5), the SAO neuroscience cross-ontology BFO chain
(`sao1785599611 ⊑ snap#Continuant`, …). This is the ENTIRE genuine
incompleteness on the ORE sample at HEAD.

| verdict | pairs | meaning |
|---|---|---|
| genuine | **10** | real calculus FN (SAO BFO chain, 2 onts) |
| masked | 307 | full tableau derives it — trust_sat/orchestrator trade, recoverable |
| dnf | 64 | full tableau >30 s/pair — budget-limited, not a calculus gap |

Per-ont: every small ont (15167=34, 7339=13, 394=12, 15682=7, 13077=2,
12253/16461/9635=1) adjudicated **0 genuine** (all masked/dnf); the 3 big
onts (4827, 14450, 13621) sampled 100 pairs each → **0 genuine** (all
masked/dnf). Only the two SAO onts are genuine.

**Headline: genuine-calculus incompleteness dropped 72 → 10** vs the
2026-06-08 baseline. The closed 62 = `ore_ont_9054`'s 60 (datatype D6–D9) +
`ore_ont_9941` xenc's 2 (snapshot fix / datatype). The residual 10 is a
SINGLE characterized pattern (cross-ontology BFO subsumption). The other
~1500 apparent silent misses are the **tunable trust_sat speed/completeness
trade** (recoverable by running the full tableau, at budget cost) — NOT
calculus gaps. (Big-ont genuine count is a 100-pair sample, not exhaustive;
300 sampled pairs across the 3 showed 0 genuine, strong evidence.)

## Net
C1 is the strong result: **FP=0 corpus-wide at ORE scale**, the snapshot
unsoundness is gone, and the session's datatype + Horn-shortcircuit work
introduced no regression (per-ont delta clean). The datatype silent channel
(the 2026-06-08 headline, ore_ont_9054) is closed. C2's residual is now
concentrated in a few large SWEET/olia ontologies + the SAO BFO cluster, whose
genuine-vs-masked split is the next measurement.
